---
// Strona główna - Dashboard
import AppLayout from '../layouts/AppLayout.astro';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

// Strona wymaga kontekstu żądania (np. nagłówków) więc musi być SSR
export const prerender = false;
---

<AppLayout 
  title="Dashboard - StravaGoals"
  description="Twój osobisty dashboard treningowy"
>
  <div class="dashboard">
    <!-- Nagłówek -->
    <div class="dashboard-header">
      <h1 class="dashboard-title">Dashboard</h1>
      <p class="dashboard-subtitle">
        Witaj z powrotem! Oto podsumowanie Twoich postępów.
      </p>
    </div>

    <!-- Placeholder karty -->
    <div class="dashboard-grid">
      <Card>
        <CardHeader>
          <CardTitle>🎯 Cele roczne</CardTitle>
          <CardDescription>Twoje cele na bieżący rok</CardDescription>
        </CardHeader>
        <CardContent>
          <p class="text-2xl font-bold">0 / 0</p>
          <p class="text-sm text-muted-foreground mt-2">
            Brak zdefiniowanych celów
          </p>
        </CardContent>
      </Card>

      <Card>
        <CardHeader>
          <CardTitle>🏃 Aktywności</CardTitle>
          <CardDescription>Aktywności w tym miesiącu</CardDescription>
        </CardHeader>
        <CardContent>
          <p class="text-2xl font-bold">0</p>
          <p class="text-sm text-muted-foreground mt-2">
            Brak aktywności
          </p>
        </CardContent>
      </Card>

      <Card>
        <CardHeader>
          <CardTitle>📈 Postęp</CardTitle>
          <CardDescription>Średni postęp celów</CardDescription>
        </CardHeader>
        <CardContent>
          <p class="text-2xl font-bold">0%</p>
          <p class="text-sm text-muted-foreground mt-2">
            Brak danych
          </p>
        </CardContent>
      </Card>

      <Card>
        <CardHeader>
          <CardTitle>⭐ Sugestie AI</CardTitle>
          <CardDescription>Rekomendacje dla Ciebie</CardDescription>
        </CardHeader>
        <CardContent>
          <p class="text-2xl font-bold">0</p>
          <p class="text-sm text-muted-foreground mt-2">
            Brak sugestii
          </p>
        </CardContent>
      </Card>
    </div>

    <!-- Informacja o funkcjonalności -->
    <Card class="mt-6">
      <CardHeader>
        <CardTitle>👋 Rozpocznij pracę</CardTitle>
        <CardDescription>
          Aby zobaczyć dane, przejdź do sekcji Ustawienia i wygeneruj przykładowe aktywności
        </CardDescription>
      </CardHeader>
      <CardContent>
        <div class="flex gap-4 flex-wrap">
          <a href="/settings" class="text-sm font-medium text-primary hover:underline">
            → Przejdź do Ustawień
          </a>
          <a href="/goals" class="text-sm font-medium text-primary hover:underline">
            → Zdefiniuj cele
          </a>
          <a href="/activities" class="text-sm font-medium text-primary hover:underline">
            → Zobacz aktywności
          </a>
        </div>
      </CardContent>
    </Card>
  </div>
</AppLayout>

<style>
  .dashboard {
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
  }

  .dashboard-header {
    margin-bottom: 2rem;
  }

  .dashboard-title {
    font-size: 2rem;
    font-weight: 700;
    color: hsl(var(--foreground));
    margin-bottom: 0.5rem;
  }

  .dashboard-subtitle {
    font-size: 1rem;
    color: hsl(var(--muted-foreground));
  }

  .dashboard-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
    margin-bottom: 1.5rem;
  }

  @media (min-width: 640px) {
    .dashboard-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .dashboard-grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }
</style>

